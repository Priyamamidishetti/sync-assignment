/**
 * connection.ts — client transport: raw WebSocket, hello handshake,
 * outbound throttling (≤30Hz, leading+trailing), reconnect with backoff.
 * Knows protocol.ts; knows nothing about rendering or interpolation.
 *
 * Phase 3.
 */
export {};
